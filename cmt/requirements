package AnalysisExamples
version ${MINERVA_RELEASE}

branches cmt src options

#==============================================================================

use GaudiKernel   v* Gaudi
use GaudiAlg      v* Gaudi

use MinervaDDDB      v*     Det
use MinervaDDDBIF    v*     Det -no_auto_imports

use MinervaEvent     v*     Event
use MinervaMCEvent   v*     Event

use ProngMaker       v*     Rec
use ParticleMaker    v*     Rec
use EnergyRecTools   v*     Rec 
use RecUtils         v*     Rec

use MinervaUtils     v*     Tools

use AnaInterfaces    v*     Ana
use AnaUtils         v*     Ana

use MomByRangeMaker  v*     Ana
#==============================================================================

# Build methods to include.<-- This is used on T2K. Perhaps it'll work here too. This doesn't work...
document doxygen doxygen -group=documentation src/*.cpp src/*.h src/*.hxx src/*.cxx ../doc/*.dox

#TransverseTools.cxx <-- How do I get this to work with for RunPostProcesses?
#library CC1P1Pi CC1P1PiAnalysis.cpp TransverseTools.cxx <-- This worked for the CC1P1PiAnalysis but not for the application

library TransverseTools TransverseTools.cxx
apply_pattern component_library library=TransverseTools

library CC1P1PiAnalysis CC1P1PiAnalysis.cpp
apply_pattern component_library library=CC1P1PiAnalysis


#Build a single library for all the cpp files in the src dir:
#library CC1P1Pi *.cpp
#Do this if you want to give the library a specific name: 

#==============================================================================

private
path_append LD_LIBRARY_PATH “$(CC1P1PIROOT)/lib”
end_private

application RunPostProcesses ../app/RunPostProcesses.cxx
